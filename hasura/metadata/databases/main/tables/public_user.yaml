table:
  schema: public
  name: user
array_relationships:
  - name: user_belongs_to_organizations
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: public
          name: user_belongs_to_organization
